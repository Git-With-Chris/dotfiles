# Brewfile for setting up Mac environment
# To install everything listed below, run:

# Before Stow: brew bundle --file=~/dotfiles/homebrew/.Brewfile
# After Stow: brew bundle --file=~/.Brewfile


# Set applications to install in /Applications
cask_args appdir: "/Applications"

# Homebrew formulae (command-line tools)
brew "git"
brew "tree"

# Homebrew casks (GUI apps)
cask "ghostty"
cask "brave-browser"
cask "raycast"
cask "visual-studio-code"
cask "pearcleaner"
cask "whatsapp"
